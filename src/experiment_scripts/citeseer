import time
from src.experiment import searchExperiment,Experiment

epr = Experiment("citeseer", mu1=5, mu2=2, sample_size=None,write_log=True,silence=False)
time_start = time.time()
epr.run(pred_method="communitude")
epr.model.loss_tracker.plot_loss()
print(f"computing_time={time.time() - time_start:.4f}")